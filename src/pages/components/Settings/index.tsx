import { Box, Typography } from "@mui/material";


  const Settings = () => {

    return (
     <Box height={100}>
        <Typography>
          Settings
        </Typography>
     </Box>
    );  
  }
  
export default Settings;